{
  "name": "grouply-api-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker + Container wrapper for the Flask backend",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.3.7"
  },
  "devDependencies": {
    "wrangler": "^4.110.0"
  }
}
